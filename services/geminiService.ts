// Demo-safe stub.
// Do NOT call Gemini directly from the browser (it would expose your API key).
export async function getMarketInsight(query: string) {
  return "Demo mode: AI assistant is disabled (no server-side key configured).";
}
